# Force Learning App

A Flutter application for force learning with popup reminders.

## Build Instructions

### Android

1. Install Flutter SDK
2. Run `flutter pub get`
3. Run `flutter build apk --release`

### iOS

1. Install Flutter SDK
2. Run `flutter pub get`
3. Run `flutter build ios --release`

## Dependencies

See pubspec.yaml for all dependencies.
